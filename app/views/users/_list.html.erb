
<div id='filterrific_results'>
  <table class='table' id='roster-table' style='table.th.text-align: left;'>
    <tr>
      <th>Name</th>
      <th>Email</th>
      <th>Team</th>
      <th>Position</th>
      <th>Schedule</th>
    </tr>
    <% users.each do |agent| %>
      <tr>
        <td><%= agent.name %></td>
        <td><%= agent.email %></td>
        <td><%= agent.team %></td>
        <td><%= agent.position %></td>
        <% begin %>
        <% shift_times = Time.parse(agent.start_time).in_time_zone('Mountain Time (US & Canada)').strftime('%I:%M %p') + " - " + Time.parse(agent.end_time).in_time_zone('Mountain Time (US & Canada)').strftime('%I:%M %p') %>
        <% rescue ArgumentError %>
        <% shift_times = "---" %>
        <% end %>
        <td><div class="strong"><%= shift_times %></div> 
        <% agent.work_days.each do |day| %>
        <%= "#{Date::ABBR_DAYNAMES[day]} " %>
      <% end %>
        </td>
        
      </tr>
    <% end %>
  </table>
  <%= will_paginate users %>
</div>



